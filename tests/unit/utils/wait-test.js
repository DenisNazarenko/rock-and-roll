import wait from 'rock-and-roll/utils/wait';
import { module, test } from 'qunit';

module('Unit | Utility | wait', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = wait();
    assert.ok(result);
  });
});
